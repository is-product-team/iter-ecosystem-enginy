{
  "name": "iter-ecosystem",
  "private": true,
  "version": "1.0.0",
  "description": "Iter Ecosystem Monorepo - Professional Architecture",
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo run clean && rm -rf node_modules **/.next **/dist **/.turbo",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "db:seed": "turbo run db:seed",
    "db:studio": "turbo run studio",
    "test": "turbo run test",
    "verify": "turbo run lint type-check test --filter=api --filter=@iter/web"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "prettier": "^3.2.5",
    "tailwindcss": "^3.4.1",
    "turbo": "^2.0.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.2.4",
  "dependencies": {
    "@expo/ngrok": "^4.1.3",
    "@prisma/client": "^5.22.0",
    "next": "15.1.1",
    "zod": "^3.24.2"
  },
  "overrides": {
    "next": "15.1.1"
  }
}
