## MODIFIED Requirements

### Requirement: Mobile Design System Consistency
The mobile application MUST provide a consistent visual experience across all screens, utilizing the centralized styling engine (NativeWind) and the shared institutional theme.

#### Scenario: Visual regression prevention
- **WHEN** the application starts
- **THEN** the Login screen SHALL display the institutional colors (dark blue, pink red) and the Apple-style Inset Grouped design as defined in the global CSS
