## 1. Implementation

- [x] 1.1 Import `logoInversImg` in `apps/web/app/[locale]/login/page.tsx`.
- [x] 1.2 Replace the single `Image` component with two theme-aware `Image` components.
- [x] 1.3 Remove the `dark:invert` class from the `Image` components.

## 2. Verification

- [x] 2.1 Verify that the logo switches correctly between light and dark modes in the login page.
- [x] 2.2 Confirm the `logo-invers.png` asset is correctly loaded in dark mode without CSS filters.
