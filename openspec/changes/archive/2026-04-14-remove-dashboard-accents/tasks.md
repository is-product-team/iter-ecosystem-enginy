## 1. Refactor Coordination Dashboard

- [x] 1.1 Remove the `absolute` "pico" div from `apps/web/app/[locale]/center/page.tsx`.
- [x] 1.2 Change `border-2` to `border` in the `CenterDashboard` cards and containers.

## 2. Refactor Admin Dashboard

- [x] 2.1 Remove the `absolute` side ribbon from `apps/web/app/[locale]/admin/page.tsx`.
- [x] 2.2 Standardize the hover border to use the normal width (`border`) and only color change.

## 3. Verification

- [x] 3.1 Verify that the dashboard cards are clean and have no decorative elements in all modes.
- [x] 3.2 Confirm that the hover interaction only changes the border color and content, without changing border width.
