## 1. Implementation

- [x] 1.1 Import `logo.png` and `logo-invers.png` statically in `apps/web/components/Navbar.tsx`.
- [x] 1.2 Update the `src` attributes of the `Image` components in the Navbar to use the imported assets.
- [x] 1.3 Remove hardcoded string paths (`"/logo.png"`, `"/logo-invers.png"`) from the Navbar component.

## 2. Verification

- [x] 2.1 Verify that the Navbar logos render correctly in the development environment.
- [x] 2.2 Run a production build (`npm run build`) in `apps/web` to ensure the static imports are resolved correctly.
