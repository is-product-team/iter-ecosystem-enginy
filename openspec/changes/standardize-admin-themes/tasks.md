## 1. Refactor Admin Pages

- [x] 1.1 Fix hardcoded colors in `apps/web/app/[locale]/requests/page.tsx`.
- [x] 1.2 Fix hardcoded colors in `apps/web/app/[locale]/verifications/page.tsx`.
- [x] 1.3 Fix hover and text colors in `apps/web/app/[locale]/workshops/page.tsx`.

## 2. Refactor Shared Admin Components

- [x] 2.1 Fix theme compatibility in `apps/web/components/CreateWorkshopModal.tsx`.
- [x] 2.2 Fix theme compatibility in `apps/web/components/CreateCenterModal.tsx`.
- [x] 2.3 Ensure `apps/web/components/Loading.tsx` uses adaptive dots when appropriate.

## 3. Verification

- [x] 3.1 Verify all admin tables look correct in Dark mode (no blinding white backgrounds).
- [x] 3.2 Confirm modals are legible and premium-looking in both modes.
