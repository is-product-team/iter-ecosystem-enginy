## 1. Audit & Core Components

- [x] 1.1 Finalize audit of all `animate-spin` and `border-t-transparent` manual spinners in `apps/web`.
- [x] 1.2 Update `Checklist.tsx` to use `Loading` component with `size="mini"`.
- [x] 1.3 Update `SyncCalendarModal.tsx` to use unified `Loading` component.

## 2. Page-level Replacements

- [x] 2.1 Update `reports/page.tsx` with standardized loader.
- [x] 2.2 Update `phases/page.tsx` with standardized loader.
- [x] 2.3 Update `verifications/page.tsx` with standardized loader.
- [x] 2.4 Update `questionnaires/page.tsx` with standardized loader.
- [x] 2.5 Update `center/requests/page.tsx` and `center/page.tsx`.

## 3. Verification & Polish

- [x] 3.1 Verify Z-index layering of `fullScreen` loader on all roles.
- [x] 3.2 Verify accessibility (ARIA labels) on the new `Loading` component.
- [x] 3.3 Final aesthetic audit for "Apple-style" consistency across all screen resolutions.
